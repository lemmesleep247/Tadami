-- Keepsake completion timestamp for an anime entry (first time the last visible episode
-- was watched in the player). Nullable: old rows and never-witnessed completions carry no date.
ALTER TABLE animes ADD COLUMN completed_at INTEGER;
