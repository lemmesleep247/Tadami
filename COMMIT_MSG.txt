cover: allow custom covers for library entries without remote URL

- AuroraCoverPlaceholders / ItemCover: non-favorite entries with blank
  cover URLs now resolve to null (skip useless fetcher calls). Favorite
  entries (library) with blank URLs remain loadable — custom covers
  and local library cache can still provide the image
- NovelCoverFetcher: return null at fetch only when URL is blank AND
  entry is not a favorite. Reorder checks to try custom cover before
  throwing on missing URL
- BrowseNovelSourceScreenModel: add browseNovelCoverUpdates state flow
  that applies resolved thumbnail URLs from network to paging data in
  real-time, so covers appear immediately while browsing
- Add/update tests for new behavior in all four modules
